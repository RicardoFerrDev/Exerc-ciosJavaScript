let celsius, fahrenheit;

fahrenheit = prompt("Informe uma temperatura em Fahrenheit para ser convertido em Celsius: ");

function temperatura(f){

        celsius = parseInt(fahrenheit - 32) / (1.8)

    return (celsius)
}
alert(temperatura(fahrenheit));